yield()强制终止线程。测试是否活动isAlive()。stop()会让线程死亡状态    【yie ld|jiːld产出放弃】
 void join(long time)引起线程等待，等调用了他的线程结束，[join|dʒɔɪn参加]
        Class SynDemo implemets Runable
        SynDemo syn = new SynDemo();
        Thread t1 = new Thread(syn);
\???是3位8进制，\u????是4位16进制 比如 char\u0000
算术运算符：+，关系运算符：==，逻辑运算符：！，位预算符&，扩展赋值运算符+=和>>>=
    条件运算符？：，点运算符.实例运算符instanceof(判断引用指向那个实例，返回布尔)，下标运算符[]
方法覆盖的原则：覆盖方法的允许访问范围不能小于原方法, 覆盖方法所抛出的异常不能比原方法更多
死锁是一种逻辑运行错误，编译器无法检测
int indexOf(int ch): 返回指定字符在字符串中第一次出现处的索引，
在swing中与JComponent类有父子关系的是Jlabel（JMenu,JButton，JTextArea都不是）
可以获得菜单项名称的是getLabel();
为识别事件对象，调用getActionCommand()返回的是事件源按钮的文字信息
AWT的font对象的作用是用font对象设置字型号
建立statement对象后，可以用executeUpdate()方法更新SQL数据 【e xe cute|ˈeksɪkjuːt实行；执行；处死】
子类继承父类的好处:子类可以拥有父类所有属性和方法。也可以扩展原有代码，生成新的类，原有代码可以不重写
Graphices2D中stroke,stroke属性控制线条的宽度，笔型样式，线段连接方式，paint属性控制填充效果【stroke|strəʊk画，划掉，中风，paint|peɪnt油漆绘画】
设置组件显示方式的是布局管理器
用javax.swing包中的类JFileChooser实现打开和保持文件对话框，首先获取的信息是文件路径。
a++ 这种先赋值，就是在这行运算范围内都是a，下一行才是--a；

19.4 错误：
    1. for(;;) 和 while(true)一样都是死循环
    2. 创建菜单项可以创建含有快捷键的菜单项
    3. 在设置环境变量后，可以输入javac 测试
    4. 除了is a 还有has a

08.10  错误
    D,B,A,A,C,D,A,D,C,A
    11.主类应用程序，
    12.if(goal==10){System.out.prient("对了!")}els{System.out.prient("错了!")}
    13.成员方法和成员变量 ---
    14.垃圾回收
    15.卡片
    16.MouseListener
    17.黄色---
    18.run
    19.BufferedRead
    20.Socket
    21.(x==2)!(y==3)
    22.第三行，第五行，第六行，
    23.，，ActionListener
    24.g.phices(20,30,100)
    25.import java.IO.*
    26.连接数据库，开启数据流，关闭流
    27.n%10,return 
    28.getContent
    29.singingItem,getActionCommad
    30.ActionEvent,
    31

